package com.pps.parapente.model;

/**
 * Utilisé en mode BAREME pour savoir si la meilleure performance est la valeur
 * la plus petite (ASC, ex: temps) ou la plus grande (DESC, ex: distance, précision en points).
 */
public enum SensClassement {
    ASC,
    DESC
}
